Para rodar Backend (no terminal): 
cd backend
npm init -y
npm install express cors
---------------------------------
Para rodar Frontend(no terminal):
npm install -g serve
cd frontend
serve -l 3000

Ai é so rodar o index.html
